# Profiles

Store one profile per user under `profiles/users/`.
Each profile should contain role, role_class, company, identity fields, and preferences.
Recommended extra context: main site, main area/process, and preferred information style.
